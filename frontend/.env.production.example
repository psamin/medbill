# Baked into the Next.js bundle at docker build time via --build-arg
# Must be the public Cloud Run URL of the backend (no trailing slash)
# Rebuild the frontend image whenever this URL changes
NEXT_PUBLIC_API_URL=https://medbill-backend-xxxx-uc.a.run.app
