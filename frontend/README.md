# MedBill Frontend

Next.js 14 App Router frontend for the medical bill processing platform.

## Setup

```bash
cd frontend
npm install
```

## Run

```bash
npm run dev
# Opens at http://localhost:3000
```

## Environment

`.env.local` points to the Flask backend. Update `NEXT_PUBLIC_API_URL` if your backend runs elsewhere.
